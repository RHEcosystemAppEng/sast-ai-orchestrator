apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sast-ai-orchestrator-dev
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/name: sast-ai-orchestrator-dev
    app.kubernetes.io/part-of: sast-ai
    environment: development
  finalizers:
    - resources-finalizer.argocd.argoproj.io/background
spec:
  project: default
  source:
    repoURL: GITHUB_REPO_URL_PLACEHOLDER
    targetRevision: main  # Track main branch for dev (use specific tag for prod)
    path: deploy/sast-ai-chart
    helm:
      releaseName: sast-ai-orchestrator-dev
      valueFiles:
        - values.yaml
        - values-dev.yaml
      parameters:
        - name: app.env.SAST_AI_WORKFLOW_NAMESPACE
          value: TARGET_NAMESPACE_PLACEHOLDER
  destination:
    server: https://kubernetes.default.svc
    namespace: TARGET_NAMESPACE_PLACEHOLDER
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
