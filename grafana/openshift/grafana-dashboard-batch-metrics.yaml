apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: mlops-batch-metrics
  labels:
    app: sast-ai-orchestrator
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  configMapRef:
    name: dashboard-mlops-batch-metrics
    key: mlops-batch-metrics.json