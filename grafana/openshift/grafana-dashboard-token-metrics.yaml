apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: mlops-token-metrics
  labels:
    app: sast-ai-orchestrator
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  configMapRef:
    name: dashboard-mlops-token-metrics
    key: mlops-token-metrics.json