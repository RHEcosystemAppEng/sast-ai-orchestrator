# Base test configuration
# Profile-specific overrides are in application-test.properties
# Test-specific datasource configuration is managed by TestProfile class

quarkus.log.level=INFO
quarkus.log.category."com.redhat.sast".level=DEBUG
